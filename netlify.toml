[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every unknown path serves index.html so client-side
# routes like /work/israeli-scouts/demo work on direct visit and refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
